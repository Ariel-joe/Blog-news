import { create } from "zustand";

const userStore = create((set) => ({
    user: null,
    isLoggedIn: false,
    login: async (userData) => {
        try {
            
        } catch (error) {
            
        }
    }
}))